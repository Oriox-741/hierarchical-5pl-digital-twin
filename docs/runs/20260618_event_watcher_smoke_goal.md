# Event Watcher Smoke Goal

Write exactly one file:

`reports/agent_loop/smoke/EVENT_WATCHER_SMOKE.md`

The file content must be exactly:

```text
EVENT_WATCHER_CODEX_SMOKE_OK
```

A trailing newline is acceptable.

Do not train, run project offline eval, run long-run gate, mutate registry,
mutate production, mutate baselines, mutate DB, mutate checkpoints, or mutate
existing eval outputs.
